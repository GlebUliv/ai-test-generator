services:
  - type: web
    name: ai-test-generator
    env: node
    plan: free
    buildCommand: npm install && mkdir -p uploads
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: "20"
